<!-- المرحلة 3 من Master Project Documentation — Core Files الجزء ب. لا يحتوي أسراراً من .env. -->

# Master Project Documentation — المرحلة 3: الملفات الأساسية (Core Files) — الجزء ب

- **نطاق هذه المرحلة:** الملفات 086–170 من قائمة Core Files المرتبة في المرحلة 1 (المجموع 170 بعد توسيع المجلدات إلى ملفات).
- **أول ملف في الجزء ب:** `nanobot/trading/agents/multi_timeframe.py`.
- **آخر ملف في الجزء ب:** `.env.example` (أسماء متغيرات فقط؛ لم يُقرأ `.env`).
- **لم يُقرأ:** `.env`. الأسماء التي تظهر هنا مأخوذة من `.env.example` أو من `os.environ.get("NAME")` في الشفرة دون قيم.
- **قاعدة USED_BY:** يُذكر كل مستورد إنتاجي تحت `nanobot/` و`webui/src/` و`scripts/` بالاسم. الاختبارات تُذكر بالعدد + المسارات إذا كان العدد ≤ 12.
- **الجزء أ:** `docs/master-project/02-core-files-a.md` (001–085).
- **المرحلة التالية (4):** كيف يفكر الوكيل — لا تبدأ إلا بعد «اكمل».

### مفتاح أعمدة الجدول

| العمود | المعنى |
|---|---|
| المسار | مسار الملف من جذر المستودع |
| الوظيفة | ماذا يفعل الملف في المنتج |
| الكلاسات / الدوال الرئيسية | الرموز ذات المستوى الأعلى |
| Imports | الاستيرادات المباشرة (بدون اختبارات TYPE_CHECKING إن أمكن) |
| يُستخدم بواسطة | من يستورد هذا الملف |

---

## 1. وكلاء التداول المتبقون (086–093)

| # | المسار | الوظيفة | الكلاسات / الدوال الرئيسية | Imports | يُستخدم بواسطة |
|---:|---|---|---|---|---|
| 086 | `nanobot/trading/agents/multi_timeframe.py` (38 سطراً) | يبني تحيز الإطار الحالي مقابل الأعلى واليومي. لا يختار شراء/بيع. `current_bias` من `run_structure_agent` على سياق السوق الحالي. `higher_bias` من اتجاه 4h (120 شمعة). `daily_bias` يُحسب من شموع 1h (120 شمعة) لا من 1d. `conflict` عندما current و higher كلاهما bullish أو bearish ومختلفان | `_trend_to_bias(trend)` يحوّل `uptrend`→`bullish` و`downtrend`→`bearish` و`range`→`neutral` وغير ذلك `unknown`؛ `run_multi_timeframe_agent(market)` → `MultiTimeframeResult` | `from __future__ import annotations`؛ `run_structure_agent`؛ `find_swings`, `infer_trend`؛ `DATA_SYMBOL`؛ `build_agent_market_context`؛ `AgentMarketContext`, `Bias`, `MultiTimeframeResult` | `nanobot/trading/orchestrator.py` |
| 087 | `nanobot/trading/agents/news_macro.py` (38 سطراً) | وكيل الأخبار/الماكرو. إذا `FOREX_FACTORY_ENABLED` في {1, true, yes} يجلب الأحداث الحية؛ وإلا تقويم فارغ وسبب stub. `news_risk` = high إذا وُجد أثر high/red وإلا medium عند وجود أحداث، وlow عند الفراغ. `trade_allowed` = False فقط عند وجود high_impact. `news_provider_configured` يعتبر التقويم مضبوطاً إذا FF مفعّل أو إذا `TRADING_NEWS_STUB` ليس في {0, false, no} (الافتراضي stub=1 أي مضبوط) | `news_provider_configured()`؛ `run_news_macro_agent()` → `NewsMacroResult` | `from __future__ import annotations`؛ `os`؛ `fetch_upcoming_events`؛ `NewsMacroResult` | `nanobot/trading/orchestrator.py`؛ `nanobot/trading/gates/build_gates.py` (يستورد `news_provider_configured`)؛ `nanobot/trading/cron.py` (يستورد `run_news_macro_agent` كسولاً) |
| 088 | `nanobot/trading/agents/risk.py` (109 أسطر) | يبني **قائمة مرشحين** للطرفين من ATR الحالي. لا يقرر الشراء/البيع. دائماً يضيف `cand-bull-1` (buy market، وقف = السعر − 1.5 ATR، أهداف +2 و+4 ATR) و`cand-bear-1` (sell market، وقف = السعر + 1.5 ATR). `quality_score` 0.65 إذا الاتجاه يوافق الطرف وإلا 0.45. كتلة demand_zone (`cand-demand-1`) لا تُنفَّذ لأن `candidates` ليست فارغة بعد الطرفين. `selected` = أعلى quality_score. `validation.accepted` إذا R:R ≥ 1.5. `selected_candidate` ليس القرار النهائي (Lonora يختار لاحقاً) | `_rr(entry, stop, target, action)`؛ `run_risk_agent(market, structure, supply_demand)` → `RiskAgentResult` | `from __future__ import annotations`؛ `AgentMarketContext`, `RiskAgentResult`, `StructureResult`, `SupplyDemandResult`, `TradeCandidate`, `TradeValidationResult` | `nanobot/trading/orchestrator.py` |
| 089 | `nanobot/trading/agents/structure.py` (25 سطراً) | وكيل الهيكل: قيعان/قمم، اتجاه، دعم/مقاومة، أحداث BOS. لا يختار طرفاً | `run_structure_agent(market)` → `StructureResult` (`trend`, `swings`, `support`, `resistance`, `structure_events`, `latest_structure_event`) | `from __future__ import annotations`؛ `detect_structure_events`, `find_swings`, `infer_trend`, `support_resistance`؛ `AgentMarketContext`, `StructureResult` | `nanobot/trading/orchestrator.py`؛ `nanobot/trading/agents/multi_timeframe.py` |
| 090 | `nanobot/trading/agents/supply_demand.py` (27 سطراً) | يبني مناطق العرض/الطلب من آخر اندفاع ATR ثم يختار أقرب demand وأقرب supply. إن لم تُصنَّف المنطقة كـ demand/supply يبحث عن منطقة تحت/فوق `last_close` | `run_supply_demand_agent(market)` → `SupplyDemandResult` | `from __future__ import annotations`؛ `build_supply_demand_zones`؛ `AgentMarketContext`, `SupplyDemandResult`, `SupplyDemandZone` | `nanobot/trading/orchestrator.py` |
| 091 | `nanobot/trading/agents/synth_prompt.py` (85 سطراً) | دستور محرّك قرار Lonora: النص الوحيد الذي يحوّل الأدلة إلى قرار. الثابت `SYNTH_SYSTEM_PROMPT` يفرض: اتجاه buy أو sell فقط (لا wait تحليلي)؛ ثلاث طبقات direction / planType / executionState؛ عقيدة دخول حرفية (تحويل conditional→immediate إذا طُبع الشرط؛ تسامح 10–15 نقطة ذهب على جهة الانتظار؛ استراتيجيات A–F)؛ المستويات من `evidenceLevels` فقط؛ الصور تؤكد الشكل لا الأسعار؛ `statisticalSupport` غير متاح؛ خرج JSON فقط بالمخطط المحدد. `synth_system_prompt(language)` يستبدل `{{LANGUAGE}}` بـ Arabic إذا بدأت اللغة بـ ar وإلا English. النص الحرفي الكامل يُنقل في المرحلة 4 | `SYNTH_SYSTEM_PROMPT` (ثابت نصي متعدد الأسطر)؛ `synth_system_prompt(language: str) -> str` | `from __future__ import annotations` فقط | `nanobot/trading/agents/synthesizer.py` |
| 092 | `nanobot/trading/agents/synthesizer.py` (257 سطراً) | المكوّن الوحيد الذي يختار buy/sell. يبني `EvidenceSnapshot` ثم يستدعي LLM (`runtime.provider.chat` أو `complete` المحقون) بدرجة حرارة 0.2 وحد أقصى 4096 رمزاً. يرسل الأدلة مجمّدة (حتى 18000 حرف) + حتى 4 لقطات رسم. إن فشل JSON يعيد طلباً واحداً للإصلاح. يدعم حلقة browse مرتين (`read_candles`, `read_zone`, غير ذلك يرفض إعادة الالتقاط). WAIT يخرج فقط عند غياب السياق أو فشل النموذج أو JSON غير صالح (حاجز تشغيلي). عند النجاح يمرّر الناتج إلى `apply_model_decision` مع السعر الحي وATR و`issued_side` | `_language_from_text`؛ `_extract_json`؛ `_runtime_complete`؛ `_browse_answer`؛ `_call_model`؛ `run_final_decision_synthesizer(...)` → `FinalDecisionResult`؛ النوع `LLMComplete` | `json`, `re`؛ `Awaitable`, `Callable`؛ `Any`؛ `current_request_context`؛ `apply_model_decision`؛ `build_evidence_snapshot`؛ `synth_system_prompt`؛ أنواع التداول؛ استيراد داخلي كسول `LiquidityResult as LQ`, `SupplyDemandResult as SD` | `nanobot/trading/orchestrator.py`؛ اختبار واحد: `tests/trading/test_lonora_cognition.py` |
| 093 | `nanobot/trading/agents/visual_capture.py` (87 سطراً) | التقاط بصري اختياري لأطر TradingView. الفشل يُخفَّض ولا يوقف التحليل. `LEAD_FRAMES` يربط الإطار القائد بأطر السياق. بدون `capture` أو عند الاستثناء: `VisualReview.state=not_checked`. جزئي إذا نُقص إطار. `checked` إذا اكتملت كل الأطر المطلوبة | `LEAD_FRAMES`؛ النوع `CaptureFn`؛ `visual_timeframes(lead)`؛ `capture_visual_evidence(lead="15m", capture=None)` → `(VisualReview, list[dict])` | `from __future__ import annotations`؛ `Awaitable`, `Callable`؛ `Any`؛ `VisualReview` | `nanobot/trading/orchestrator.py` |

---

## 2. البوتات والبطاقات والجدل والرسوم (094–102)

| # | المسار | الوظيفة | الكلاسات / الدوال الرئيسية | Imports | يُستخدم بواسطة |
|---:|---|---|---|---|---|
| 094 | `nanobot/trading/bots/__init__.py` (سطر واحد) | علامة حزمة: «Autonomous bot scanners.» لا يعيد تصدير رموز | لا رموز | لا استيراد | لا مستورد إنتاجي |
| 095 | `nanobot/trading/bots/coordinator.py` (59 سطراً) | منسّق ماسحات بوتات بأسلوب foxagent. الافتراضي `enabled_agents=["news_candle", "multi_strategy", "pattern_notes"]`. `news_candle` مذكور ولا يُنفَّذ. `multi_strategy`: إن كان جسم آخر شمعة > 0.8 ATR يصدر إشارة buy/sell. `pattern_notes`: إن كان مدى آخر 10 شموع < 2 ATR يضيف تنبيه ضغط. يجلب سياق السوق و`fetch_quote("XAUUSD")` | `BotSignal` (`agent`, `symbol`, `direction`, `reason`, `price`, `ts`)؛ `BotCycleResult` (`signals`, `alerts`)؛ `run_bot_cycle(enabled_agents=None)` | `from __future__ import annotations`؛ `time`؛ `dataclass`, `field`؛ `Any`؛ `build_agent_market_context`؛ `fetch_quote` | `nanobot/trading/cron.py` |
| 096 | `nanobot/trading/cards/__init__.py` (سطر واحد) | علامة حزمة: «Card projection from AgentFinalResult.» لا يعيد تصدير | لا رموز | لا استيراد | لا مستورد إنتاجي |
| 097 | `nanobot/trading/cards/derive.py` (108 أسطر) | يسقط بطاقات واجهة من `AgentFinalResult` دون تأليف LLM. الترتيب: scenario_notice, decision, plan_levels, activation, invalidation, gate_checklist, visual_review, key_reasons, risk_warnings, tracked_recommendation. يضيف decision دائماً. يضيف plan_levels/activation/invalidation فقط عند buy/sell مع entry وstop وtargets. visual_review عند buy/sell. tracked_recommendation إذا وُجد `recommendation_id` | `CARD_ORDER`؛ `derive_cards(result)` → `list[dict]` | `from __future__ import annotations`؛ `Any`؛ `AgentFinalResult` | `nanobot/trading/orchestrator.py`؛ اختبار: `tests/trading/test_cards.py` |
| 098 | `nanobot/trading/cards/format.py` (193 سطراً) | تنسيق بطاقة توصية عربية أولاً لتيليجرام (HTML) وواتساب (ماركداون خفيف). يترجم الاتجاه/التحيز/الإعداد؛ يسعر بـ `$x,xxx.xx`؛ يحسب R:R من الملخص أو من المستويات؛ يعرض حتى سببين/أهداف 2؛ يذكر نجاح أو حجب G1-G7؛ يذيّل «توصيات فقط — بدون تنفيذ.» | الثوابت `_TREND_AR`, `_BIAS_AR`, `_SETUP_AR`, `_DECISION`, `_RR_RE`؛ `format_price`؛ `_decision_parts`؛ `_confidence_pct`؛ `_recommendation`؛ `compute_rr`؛ `translate_reason`؛ `_reasons`؛ `_gates_passed`؛ `render_telegram_card`؛ `render_whatsapp_card` | `from __future__ import annotations`؛ `html`؛ `re`؛ `Any` | `nanobot/channels/telegram/trading_cards.py`؛ `nanobot/channels/whatsapp/trading_cards.py`؛ اختبار: `tests/trading/test_recommendation_cards.py` |
| 099 | `nanobot/trading/crew/__init__.py` (سطر واحد) | علامة حزمة: «Debate crew (foxagent pattern).» | لا رموز | لا استيراد | لا مستورد إنتاجي |
| 100 | `nanobot/trading/crew/debate.py` (50 سطراً) | طاقم مناظرة صوري: رسائل technical/bull/bear/risk ثابتة ثم يشغّل `run_unified_chart_agent(team_mode="debate")`. لا يغيّر القرار. يطلق مرحلتي research running/done | النوع `DebateRole`؛ `DebateMessage`؛ `DebateResult`؛ `run_debate_crew(user_message="", emit=None)` | `from __future__ import annotations`؛ `dataclass`, `field`؛ `Any`, `Literal`؛ `run_unified_chart_agent`؛ `StageEvent`, `emit_stage`؛ `AgentFinalResult` | `nanobot/agent/tools/trading_chart.py`؛ `nanobot/trading/fast_path.py`؛ `nanobot/webui/trading_api.py` |
| 101 | `nanobot/trading/drawings/__init__.py` (سطر واحد) | علامة حزمة: «Drawing plan builders.» | لا رموز | لا استيراد | لا مستورد إنتاجي |
| 102 | `nanobot/trading/drawings/plan.py` (132 سطراً) | يبني رسوم الشارت: حتى خطّي دعم وخطّي مقاومة، منطقة طلب واحدة، مسار سيناريو أساسي (لون `#38bdf8`)، مسار بديل متقطع (`#f97316` ينتهي عند الوقف)، خط إبطال. حد أقصى 12 كائناً. `bar_ms` ثابت 15 دقيقة. `_path_points` يبدأ من سعر/وقت الدخول ويضمن تزايد الزمن | `_MAX_OBJECTS = 12`؛ `_path_points(waypoints, start_price, start_time, bar_ms)`؛ `build_drawing_plan(structure, supply_demand, decision, market=None)` → `list[ChartDrawing]` | `from __future__ import annotations`؛ `AgentMarketContext`, `ChartDrawing`, `FinalDecisionResult`, `ScenarioWaypoint`, `StructureResult`, `SupplyDemandResult` | `nanobot/trading/orchestrator.py`؛ اختبار: `tests/trading/test_lonora_cognition.py` |

---

## 3. البوابات والهندسة والذاكرة والأخبار (103–116)

| # | المسار | الوظيفة | الكلاسات / الدوال الرئيسية | Imports | يُستخدم بواسطة |
|---:|---|---|---|---|---|
| 103 | `nanobot/trading/gates/__init__.py` (6 أسطر) | يعيد تصدير سلسلة البوابات | `__all__ = ["build_gates", "run_gate_chain"]` | `run_gate_chain` من `chain`؛ `build_gates` من `build_gates` | لا مستورد إنتاجي يستورد الحزمة نفسها (المستوردون يستخدمون الوحدات الفرعية مباشرة) |
| 104 | `nanobot/trading/gates/build_gates.py` (120 سطراً) | يعرّف G1–G7. `GATE_REQUIRED`: G1 True، G2 False، G3 False، G4 True، G5 False، G6 True، G7 True. G1: نافذة أخبار عالية الأثر أو unavailable. G2: وجود خريطة سيولة فقط ثم pass. G3: وجود عرض/طلب فقط ثم pass. G4: وجود هيكل؛ يخصم 10 إذا تعارض MTF أو لا أطر بصرية. G5 اسمه «Backtest (removed)» ويمرّ دائماً. G6: تماسك الدخول. G7: إعادة تحقق السعر الحي (قد يعيد تثبيت الدخول). البوابات لا تقلب الطرف | `GATE_REQUIRED`؛ `GateDefinition(id, name, run)`؛ `GateInputs(now_ms, news, structure, liquidity, supply_demand, mtf, plan, atr, visual_timeframes, fetch_live_price)`؛ `build_gates(inp)` → `list[GateDefinition]` داخله `g1`–`g7` | `from __future__ import annotations`؛ `time`؛ `Awaitable`, `Callable`؛ `dataclass`؛ `Any`؛ `news_provider_configured`؛ `validate_entry_coherence`؛ `evaluate_news_window`؛ `revalidate_plan`؛ `EntryPlan`, `GateStatus`, `LiquidityResult`, `MultiTimeframeResult`, `NewsMacroResult`, `StructureResult`, `SupplyDemandResult` | `nanobot/trading/gates/__init__.py`؛ `nanobot/trading/gates/chain.py`؛ `nanobot/trading/gates/reprice_loop.py`؛ `nanobot/trading/orchestrator.py`؛ اختباران: `tests/trading/test_gates.py`, `tests/trading/test_reprice_loop.py` |
| 105 | `nanobot/trading/gates/chain.py` (50 سطراً) | يشغّل البوابات بالترتيب ويتوقف عند veto أو عند unavailable لبوابة مطلوبة. يجمع `confidence_delta`. الاستثناء داخل بوابة يصبح unavailable | `run_gate_chain(gates)` → `GateChainResult` | `from __future__ import annotations`؛ `asyncio` (مستورد وغير مستخدم)؛ `time`؛ `GATE_REQUIRED`, `GateDefinition`؛ `GateChainResult`, `GateId`, `GateVerdict`, `GateStatus` | `nanobot/trading/gates/__init__.py`؛ `nanobot/trading/gates/reprice_loop.py`؛ `nanobot/trading/orchestrator.py`؛ اختبار: `tests/trading/test_gates.py` |
| 106 | `nanobot/trading/gates/entry_semantics.py` (24 سطراً) | G6: تماسك هندسة الصفقة. يرفض إن نقص entry/stop/targets. شراء: الوقف تحت الدخول. بيع: الوقف فوق الدخول. كل هدف أبعد من 25 ATR مرفوض. `DEFAULT_MAX_ATR_DISTANCE = 0.3` معرّف وغير مستخدم في الدالة | `DEFAULT_MAX_ATR_DISTANCE`؛ `MAX_TARGET_ATR_DISTANCE = 25`؛ `validate_entry_coherence(plan, atr)` → `(bool, list[str])` | `from __future__ import annotations`؛ `EntryPlan` | `nanobot/trading/gates/build_gates.py`؛ اختبار: `tests/trading/test_gates.py` |
| 107 | `nanobot/trading/gates/news_window.py` (43 سطراً) | G1: تعتيم 30 دقيقة قبل حدث high و15 دقيقة بعده. يتجاهل أثر غير high وأوقات غير قابلة للتحليل | `BLACKOUT_BEFORE_MS`؛ `BLACKOUT_AFTER_MS`؛ `NewsWindowVerdict`؛ `_parse_event_time`؛ `evaluate_news_window(events, now_ms)` | `from __future__ import annotations`؛ `dataclass`؛ `UTC`, `datetime` من `datetime` (`UTC` مستورد وغير مستخدم)؛ `EconomicEvent` | `nanobot/trading/gates/build_gates.py` |
| 108 | `nanobot/trading/gates/reprice_loop.py` (65 سطراً) | إذا G7 أعاد تثبيت الدخول على السعر الحي: يحدّث `plan.entry` و`recommendation.entry` ويجعل الخطة immediate/market/`valid_now` ويمسح شروط التفعيل، ثم يعيد تشغيل G6 ثم G7 حتى جولتين (`MAX_REPRICE_ROUNDS = 2`). يدمج الأحكام. لا يقلب الطرف | `_merge_gate_chains`؛ `apply_g7_reprice_loop(chain, gates, plan, recommendation)` | `from __future__ import annotations`؛ `GateDefinition`؛ `run_gate_chain`؛ `AgentRecommendation`, `EntryPlan`, `GateChainResult`, `GateVerdict` | `nanobot/trading/orchestrator.py` (استيراد كسول داخل الدالة)؛ اختبار: `tests/trading/test_reprice_loop.py` |
| 109 | `nanobot/trading/gates/revalidation.py` (50 سطراً) | G7: يتحقق من السعر الحي مقابل الخطة. unavailable إذا لا سعر. invalidated إذا اخترق الوقف. targets_passed إذا تجاوز كل الأهداف. reanchored إذا الانزلاق > 0.5 ATR. وإلا ok | النوع `RevalidationStatus`؛ `RevalidationResult`؛ `MAX_SLIPPAGE_ATR = 0.5`؛ `revalidate_plan(plan, live_price, atr)` | `from __future__ import annotations`؛ `dataclass`؛ `Literal`؛ `EntryPlan` | `nanobot/trading/gates/build_gates.py` |
| 110 | `nanobot/trading/geometry/__init__.py` (سطر واحد) | علامة حزمة: «Chart geometry from OHLC.» لا يعيد تصدير | لا رموز | لا استيراد | لا مستورد إنتاجي |
| 111 | `nanobot/trading/geometry/detectors.py` (172 سطراً) | هندسة OHLC الحتمية: ATR(14)، أرجوحات يسار/يمين 2، اتجاه HH+HL / LH+LL وإلا range، أقرب 5 دعم و5 مقاومة، BOS إذا أغلق آخر شمعة فوق آخر قمة أو تحت آخر قاع، مستويات متساوية بتسامح 0.0008، كنس سيولة (wick يتجاوز ثم يغلق داخل)، منطقة عرض/طلب من اندفاع آخر شمعة > 0.6 ATR باستخدام قاعدة الشمعة السابقة | `compute_atr`؛ `find_swings`؛ `infer_trend`؛ `support_resistance`؛ `detect_structure_events`؛ `find_equal_levels`؛ `detect_sweeps`؛ `build_supply_demand_zones` | `from __future__ import annotations`؛ `Candle`, `LiquiditySweep`, `PriceLevel`, `StructureEvent`, `SupplyDemandZone`, `Swing`, `TrendLabel` | `nanobot/trading/market_context.py` (`compute_atr`)؛ `nanobot/trading/agents/structure.py`؛ `nanobot/trading/agents/multi_timeframe.py`؛ `nanobot/trading/agents/supply_demand.py`؛ `nanobot/trading/agents/liquidity.py` |
| 112 | `nanobot/trading/geometry/snapshot.py` (38 سطراً) | لقطة هندسية من آخر 3 قمم و3 قيعان: خط مقاومة وخط دعم وحالة rising/falling. `channels` و`patterns` قائمتان فارغتان دائماً. المصدر `deterministic_swings`. دليل لا قرار | `build_geometry_snapshot(structure)` → `dict` فيه `trendlines`, `channels`, `patterns`, `source` | `from __future__ import annotations`؛ `Any`؛ `StructureResult` | `nanobot/trading/orchestrator.py` |
| 113 | `nanobot/trading/memory/__init__.py` (16 سطراً) | يعيد تصدير ذاكرة القرارات | يعيد `format_decisions_for_dream`, `list_recent_decisions`, `record_trade_decision`, `trades_memory_path` | الاستيراد من `nanobot.trading.memory.decisions` | لا مستورد إنتاجي للحزمة نفسها (الاستدعاءات تستورد `decisions.py` مباشرة) |
| 114 | `nanobot/trading/memory/decisions.py` (82 سطراً) | ذاكرة قرارات JSONL إضافية في `get_data_dir()/trading/trades.jsonl`. `record_trade_decision` يلحق سطراً واحداً (id, ts, symbol, interval, direction, confidence, summary, entry, stop_loss, targets, execution_state, quote_mid, key_reasons[:5], risk_warnings[:5]). `list_recent_decisions` يقرأ من النهاية. `format_decisions_for_dream` يبني مقطع Markdown لحلم الوكيل | `trades_memory_path`؛ `record_trade_decision`؛ `list_recent_decisions`؛ `format_decisions_for_dream` | `from __future__ import annotations`؛ `json`؛ `time`؛ `Path`؛ `Any`؛ `get_data_dir`؛ `AgentMarketContext`, `FinalDecisionResult` | `nanobot/trading/recommendations/store.py` (`record_trade_decision`)؛ `nanobot/agent/memory.py` (`format_decisions_for_dream`)؛ `nanobot/webui/trading_api.py` (`list_recent_decisions`)؛ `nanobot/trading/memory/__init__.py`؛ اختبار: `tests/trading/test_decision_memory.py` |
| 115 | `nanobot/trading/news/__init__.py` (سطر واحد) | علامة حزمة: «News providers for gold macro agent.» | لا رموز | لا استيراد | لا مستورد إنتاجي |
| 116 | `nanobot/trading/news/forex_factory.py` (47 سطراً) | تغذيه تقويم Forex Factory. لا يطلب الشبكة إلا إذا `FOREX_FACTORY_ENABLED` في {1, true, yes}. العنوان `https://nfs.faireconomy.media/ff_calendar_thisweek.json` عبر `urllib.request.urlopen` بمهلة 8 ثوانٍ. يبقي عملات USD/XAU/ALL وأثر high/medium/red/orange. حد 12 حدثاً. أي استثناء يعيد قائمة فارغة | `_CALENDAR_URL`؛ `_GOLD_CURRENCIES`؛ `fetch_upcoming_events(limit=12)` | `from __future__ import annotations`؛ `json`؛ `os`؛ `urllib.request`؛ `Any` | `nanobot/trading/agents/news_macro.py`؛ اختبار: `tests/trading/test_forex_factory.py` |

---

## 4. التوصيات والفرق (117–126)

| # | المسار | الوظيفة | الكلاسات / الدوال الرئيسية | Imports | يُستخدم بواسطة |
|---:|---|---|---|---|---|
| 117 | `nanobot/trading/recommendations/__init__.py` (سطر واحد) | علامة حزمة: «Recommendation persistence.» | لا رموز | لا استيراد | لا مستورد إنتاجي |
| 118 | `nanobot/trading/recommendations/followup.py` (127 سطراً) | متابعة الخطة الحيّة: لا يستدعي الـ synthesizer. `latest_open_recommendation` يبحث في آخر 5 صفوف عن status في {valid_now, awaiting_activation}. `explain_stored_recommendation` يشرح صفاً مخزناً. `grade_live_recommendation` يجلب سعر OANDA الحي ويصنّف waiting / in_trade / tp1 / invalidated حسب الطرف؛ الملخص عربي إذا وُجدت حروف عربية؛ يحذّر أن هذه متابعة لا توصية ثانية | `latest_open_recommendation`؛ `explain_stored_recommendation`؛ `grade_live_recommendation` | `from __future__ import annotations`؛ `re`؛ `DATA_SYMBOL`؛ `fetch_quote`؛ `get_recommendation`, `list_recommendations`؛ `AgentRecommendation`, `FinalDecisionResult` | `nanobot/trading/orchestrator.py` (`grade_live_recommendation`)؛ `nanobot/trading/fast_path.py` (`grade_live_recommendation`)؛ `nanobot/trading/cron.py` (`latest_open_recommendation`)؛ `nanobot/webui/trading_api.py` (`latest_open_recommendation`). `explain_stored_recommendation` لا مستورد إنتاجي خارج الملف |
| 119 | `nanobot/trading/recommendations/store.py` (194 سطراً) | تخزين SQLite في `get_data_dir()/trading/recommendations.db`. الجدول `recommendations`: id, symbol, interval, direction, entry, stop_loss, targets_json, status, summary, confidence, drawings_json, gate_json, created_at, session_key (يُضاف ALTER إن نقص). `store_recommendation` يرفض إن لم تكن الخطة قابلة للتداول أو إن وُجدت خطة حيّة لنفس `session_key` (توصية حيّة واحدة لكل محادثة). بعد الإدراج يستدعي `record_trade_decision`. `latest_live_recommendation(session_key)` يقيّد بـ session_key وstatus في {valid_now, awaiting_activation} | `_db_path`؛ `_conn`؛ `store_recommendation`؛ `get_recommendation`؛ `latest_live_recommendation`؛ `list_recommendations` | `from __future__ import annotations`؛ `json`؛ `sqlite3`؛ `time`؛ `uuid`؛ `Path`؛ `get_data_dir`؛ `assess_plan_tradability`؛ `AgentMarketContext`, `ChartDrawing`, `FinalDecisionResult`؛ استيراد كسول `record_trade_decision` | `nanobot/trading/orchestrator.py`؛ `nanobot/trading/fast_path.py`؛ `nanobot/trading/gold_intent_context.py`؛ `nanobot/trading/recommendations/followup.py`؛ `nanobot/webui/trading_api.py` (`list_recommendations`)؛ اختبار: `tests/trading/test_lonora_cognition.py` |
| 120 | `nanobot/trading/recommendations/tradability.py` (22 سطراً) | يقرر هل تُخزَّن الخطة: يرفض wait، رفض البوابات، طرف غير buy/sell، مستويات ناقصة أو أسعار ≤ 0 | `assess_plan_tradability(decision)` → `(bool, str)` | `from __future__ import annotations`؛ `FinalDecisionResult` | `nanobot/trading/recommendations/store.py`؛ اختبار: `tests/trading/test_lonora_cognition.py` |
| 121 | `nanobot/trading/teams/models.py` (33 سطراً) | نماذج DAG للأسراب: وكيل، مهمة، إعداد مسبق | `SwarmAgent(id, role, system_prompt="")`؛ `SwarmTask(id, agent_id, prompt_template, depends_on, input_from)`؛ `SwarmPreset(name, title, description, agents, tasks, variables)` | `from __future__ import annotations`؛ `dataclass`, `field`؛ `Any` | `nanobot/trading/teams/runtime.py` |
| 122 | `nanobot/trading/teams/runtime.py` (83 سطراً) | منفّذ YAML: يحمّل الإعداد من `teams/presets/*.yaml`، يرتّب المهام طبقياً ( Kahn / indegree )، ينفّذ كل طبقة بـ `asyncio.gather` لكن جسم المهمة يملأ قالباً نصياً فقط (`[role] prompt[:500]`) دون استدعاء متخصصين. بعدها يشغّل `run_unified_chart_agent(team_mode=f"swarm:{preset_name}")` وهو مصدر القرار الفعلي. المتغيرات الافتراضية `target=XAUUSD`, `market=forex` | `list_presets`؛ `load_preset`؛ `topological_layers`؛ `run_swarm` | `from __future__ import annotations`؛ `asyncio`؛ `defaultdict`, `deque` (`defaultdict` مستورد وغير مستخدم)؛ `Path`؛ `Any`؛ `yaml`؛ `run_unified_chart_agent`؛ `SwarmAgent`, `SwarmPreset`, `SwarmTask`؛ `AgentFinalResult` (مستورد وغير مستخدم) | `nanobot/agent/tools/trading_chart.py`؛ `nanobot/agent/tools/trading_team.py`؛ `nanobot/trading/fast_path.py`؛ `nanobot/webui/trading_api.py`؛ اختبار: `tests/trading/test_teams.py` |
| 123 | `nanobot/trading/teams/presets/gold_analysis_committee.yaml` (48 سطراً) | إعداد لجنة تحليل: وكلاء macro_analyst, structure_analyst, liquidity_analyst, risk_officer, lead_analyst. مهام متوازية macro/structure/liquidity ثم risk ثم lead. متغير مطلوب: `target` | YAML: `name`, `title`, `description`, `agents`, `tasks`, `variables` | لا ينطبق (YAML) | `load_preset("gold_analysis_committee")` عبر `runtime.py`؛ مطابقة نية في `nanobot/trading/intent_router.py` للنمط `(committee\|لجنة)`؛ أداة `run_trading_team` |
| 124 | `nanobot/trading/teams/presets/gold_debate_desk.yaml` (40 سطراً) | إعداد مناظرة: bull_advocate وbear_advocate متوازيان ثم risk_manager. لكل من bull/bear `system_prompt`. متغيرات: `target` مطلوب، `market` اختياري | YAML كما أعلاه | لا ينطبق | `load_preset("gold_debate_desk")`؛ `intent_router.py` للنمط `(debate desk\|مناظرة)`؛ `tests/trading/test_teams.py` |
| 125 | `nanobot/trading/teams/presets/gold_mtf_panel.yaml` (40 سطراً) | لوحة أطر: h1_analyst, h4_analyst, d1_analyst متوازية ثم mtf_synthesizer. متغير `target` مطلوب | YAML كما أعلاه | لا ينطبق | `load_preset("gold_mtf_panel")`؛ `intent_router.py` للنمط `(mtf panel\|لوحة)` |
| 126 | `nanobot/trading/teams/presets/gold_news_war_room.yaml` (33 سطراً) | غرفة أخبار: news_scanner ثم event_analyst ثم scenario_planner. متغير `target` مطلوب | YAML كما أعلاه | لا ينطبق | `load_preset("gold_news_war_room")`؛ `intent_router.py` للنمط `(war room\|غرفة الأخبار)`؛ `tests/trading/test_fast_path.py` (`test_resolve_team_preset_war_room`) |

---

## 5. أدوات الوكيل والمهارات (127–132)

| # | المسار | الوظيفة | الكلاسات / الدوال الرئيسية | Imports | يُستخدم بواسطة |
|---:|---|---|---|---|---|
| 127 | `nanobot/agent/tools/trading_chart.py` (158 سطراً) | أداتا الذهب المكتشفتان عبر `ToolLoader` ضمن `_GOLD_AGENT_MODULES`. `get_gold_quote`: سعر OANDA الحي (رمز ذهب فقط) ويُرجع JSON bid/ask/mid/tradeable. `analyze_gold`: يفتح الشارت عبر `TradingStagePublisher.open_chart` ثم يشغّل core أو debate (`run_debate_crew`) أو swarm (`run_swarm`) ثم `result_to_wire` و`publish_result`. معاملات التحليل: interval في {1m,5m,15m,30m,1h,4h,1d} وteam_mode في {core,debate,swarm} وpreset اختياري | `_QUOTE_PARAMETERS`؛ `_ANALYZE_PARAMETERS`؛ `_request_route`؛ `GetGoldQuoteTool` (`name=get_gold_quote`, `read_only=True`)؛ `AnalyzeGoldTool` (`name=analyze_gold`) | `from __future__ import annotations`؛ `json`؛ `TYPE_CHECKING`, `Any`؛ `Tool`, `ToolResult`, `tool_parameters`؛ `ToolContext`, `current_request_context`؛ `StringSchema`, `tool_parameters_schema`؛ `load_trading_config`؛ `run_debate_crew`؛ `DATA_SYMBOL`, `GoldOnlyError`؛ `fetch_quote`؛ `run_unified_chart_agent`؛ `result_to_wire`؛ `TradingStagePublisher`؛ `run_swarm`؛ تحت TYPE_CHECKING: `MessageBus` | يكتشفه `nanobot/agent/tools/loader.py` بالاسم `trading_chart`؛ `nanobot/agent/tools/trading_team.py` يستورد `AnalyzeGoldTool` (استيراد غير مستخدم في الجسم)؛ اختبار: `tests/trading/test_trading_tools.py` |
| 128 | `nanobot/agent/tools/trading_team.py` (96 سطراً) | أداة `run_trading_team`: تشغّل إعداد YAML ثم تنشر النتيجة. تتطلب `RequestContext` حيّاً. presets المسموحة في المخطط: gold_analysis_committee, gold_debate_desk, gold_news_war_room, gold_mtf_panel. الوصف يعرض `list_presets()`. لا يستخدم `AnalyzeGoldTool` المستورد | `_TEAM_PARAMETERS`؛ `RunTradingTeamTool` (`name=run_trading_team`) | `from __future__ import annotations`؛ `json`؛ `Any`؛ `Tool`, `ToolResult`, `tool_parameters`؛ `ToolContext`, `current_request_context`؛ `StringSchema`, `tool_parameters_schema`؛ `AnalyzeGoldTool` (غير مستخدم)؛ `list_presets`, `run_swarm`؛ استيراد داخلي `result_to_wire`, `TradingStagePublisher` | يكتشفه `nanobot/agent/tools/loader.py` بالاسم `trading_team`؛ يُذكر في `nanobot/skills/gold-trading/SKILL.md` و`nanobot/trading/gold_intent_context.py`. لا استيراد إنتاجي للوحدة نفسها |
| 129 | `nanobot/skills/gold-trading/SKILL.md` (38 سطراً) | دستور الوكيل الظاهر للنموذج: ذهب فقط، توصيات فقط، القرار التحليلي BUY أو SELL (لا WAIT من الوكيل)، المتخصصون لا يختارون الطرف، البوابات قد ترفض ولا تقلب، توصية حيّة واحدة لكل محادثة، الصور تؤكد الشكل، أدوات `get_gold_quote` / `analyze_gold` / `run_trading_team`، لا أسرار ولا حقن أوامر. frontmatter: `name: gold-trading` | مستند مهارة (YAML frontmatter + Markdown) | لا ينطبق | يحمّله `nanobot/agent/skills.py` من `BUILTIN_SKILLS_DIR` (`nanobot/skills`). لا استيراد بايثون مباشر |
| 130 | `nanobot/skills/README.md` (31 سطراً) | فهرس مهارات مضمّنة بأسلوب OpenClaw. يصف صيغة SKILL.md. جدول «Available Skills» يذكر `github`, `weather`, `summarize`, `tmux`, `clawhub`, `skill-creator` ولا يذكر `gold-trading` ولا `memory` ولا `cron` (عدم تطابق مع المجلد الحالي) | Markdown | لا ينطبق | توثيق بشري؛ اختبارات filesystem تشير إلى مسارات مهارات عامة لا إلى هذا الملف تحديداً كمنطق إنتاج |
| 131 | `nanobot/skills/memory/SKILL.md` (20 سطراً) | مهارة البحث في سجل التاريخ JSONL عبر أداة `grep` (count ثم تضييق، `fixed_strings` للطوابع). لا يحمّل السجل كاملاً في السياق. frontmatter: `name: memory` | مستند مهارة | لا ينطبق | يحمّله `nanobot/agent/skills.py` من `BUILTIN_SKILLS_DIR` |
| 132 | `nanobot/skills/cron/SKILL.md` (59 سطراً) | مهارة الجدولة: أوضاع Reminder / Task / One-time. أمثلة `every_seconds` و`cron_expr` و`tz` و`at`. يوجّه الفحوصات الصامتة إلى `HEARTBEAT.md` لا إلى cron. جدول تحويل عبارات الوقت. frontmatter: `name: cron` | مستند مهارة | لا ينطبق | يحمّله `nanobot/agent/skills.py`؛ اختبار يقرأ المسار `skills/cron/SKILL.md` في `tests/tools/test_filesystem_tools.py` |

---

## 6. واجهة WebUI للتداول (133–152)

| # | المسار | الوظيفة | الكلاسات / الدوال الرئيسية | Imports | يُستخدم بواسطة |
|---:|---|---|---|---|---|
| 133 | `webui/src/main.tsx` (48 سطراً) | مدخل React: يعوّض `crypto.randomUUID` على HTTP غير الآمن، يهيّئ i18n وloopback runtime host، يرندر `App` دون StrictMode (حتى لا تُضاعف محدّثات الدلتا)، يسجّل `/sw.js` عند التحميل | `renderWebui`؛ تعويض `randomUUID` | `react-dom/client`؛ `./App`؛ `./globals.css`؛ `./i18n` (`initializeI18n`)؛ `./lib/runtime` (`initializeLoopbackRuntimeHost`) | نقطة دخول Vite (`index.html` → هذا الملف) |
| 134 | `webui/src/App.tsx` (3072 سطراً) | صدفة التطبيق كاملة: إقلاع، مصادقة bootstrap، جلسات، إعدادات، لوحة العمل، وواجهات التداول. مسارات التداول: `/chart` → `GoldChartPanel`؛ `/performance` → `TradingPerformance`؛ `/briefing` → `TradingBriefingPanel`؛ `/recommendations` و`/inbox` → `TradingInbox`؛ `/connect` و`/channels` → `TradingConnect`. على سطح المكتب يفتح جزء شارت عبر `tradingChartPaneKey` عندما `chartOpen`. على الجوال يرندر `TradingChartBottomSheet`. تحميل كسول لكل ألواح التداول | `BootState`؛ ثوابت التخزين والعرض؛ `ThreadShell` و`SettingsView` و`GoldChartPanel` و`TradingChartSidecar` و`TradingInbox` و`TradingPerformance` و`TradingBriefingPanel` و`TradingChartBottomSheet` و`TradingConnect` (كلها `lazy`)؛ `SurfaceLoadingFallback`؛ `isSettingsSectionKey`؛ `defaultShellRoute`؛ `readShellRoute`؛ `shellRouteHash`؛ `writeShellRoute`؛ `AuthForm`؛ `HostChrome`؛ `PairingCodePopup`؛ `PairingChannelBadge`؛ `PairingCodeSlots`؛ `App` (default export)؛ `Shell` | `react` (`lazy`, `Suspense`, `useCallback`, `useEffect`, `useMemo`, `useRef`, `useState`, `ReactNode`)؛ `lucide-react` (`Eye`, `EyeOff`, `Moon`, `ShieldCheck`, `Sun`, `X`)؛ `react-i18next`؛ `channelUiPresentation`؛ `Sidebar`؛ `SidebarResizeHandle`, `SIDEBAR_MIN_WIDTH`, `SIDEBAR_MAX_WIDTH`؛ `matchSidebarShortcut`؛ أنواع `SidebarDeleteItem`, `SettingsSectionKey`, `SettingsExitGuard`؛ `ThreadVisibilityContext`؛ `PaneWorkbench`؛ نموذج workbench (`MAX_WORKBENCH_PANES`, `addWorkbenchPane`, `attachWorkbenchPane`, `createWorkbenchTab`, `detachWorkbenchPane`, `dissolveWorkbenchTab`, `orderWorkbenchTabs`, `reconcileWorkbench`, `renameWorkbenchTab`, `setWorkbenchLayout`, `setWorkbenchPaneLayoutOrder`, `setWorkbenchSplitRatios`, `workbenchTab`, `workbenchTabForPane`, `WorkbenchState`)؛ من session-store: `anchorPaneKeyFromTradingChart`, `getTradingSession`, `isTradingChartPaneKey`, `subscribeTradingSession`, `tradingChartPaneKey`؛ `floatingSurfaceElevationClassName`؛ `Sheet`, `SheetContent`, `SheetTitle`؛ hooks: `useSessions`, `useDeferredTitleRefresh`, `useSidebarState`, `useSkills`, `useLogoFallback`, `usePageVisibility`, `useMediaQuery`, `ThemeProvider`, `useTheme`؛ `logoFallbackUrls`؛ `cn`؛ bootstrap (`BootstrapAuthRequiredError`, `clearSavedSecret`, `consumeUrlBootstrapSecret`, `deriveWsUrl`, `fetchBootstrap`, `loadSavedSecret`, `saveSecret`)؛ `displayTitle`, `sortSessions`؛ `deriveTitle`؛ `NanobotClient`؛ `ClientProvider`, `useClient`؛ أنواع `BootstrapResponse`, `ChatSummary`, `RuntimeSurface`, `PairingRequestInfo`, `SessionAutomationJob`, `SettingsPayload`, `WorkspaceScopePayload`, `WorkspacesPayload`؛ `Button`؛ `Input`؛ `fetchPairingRequests`, `fetchSettings`, `fetchWorkspaces`, `runPairingAction`؛ `createRuntimeHost`, `isNativeRuntime`, `toRuntimeSurface`؛ `projectNameFromPath`, `scopeWithAccessMode`؛ `createTemporaryChatSession`, `deriveTemporaryChatTitle` | يُرندر من `webui/src/main.tsx` |
| 135 | `webui/src/components/trading/TvChart.tsx` (164 سطراً) | غلاف TradingView Advanced Charts. الرمز الافتراضي `XAUUSD`. يحمّل `/charting_library/charting_library.standalone.js` مرة واحدة. يعطّل بحث الرموز والمقارنة. datafeed عبر `createTradingDatafeed`. تحويل الفواصل: 1m→1، 5m→5، 15m→15، 30m→30، 1h→60، 4h→240، 1d→1D، 1w→1W. ثيم داكن، locale en، autosize | `DATA_SYMBOL`؛ `LIBRARY_PATH`؛ `SCRIPT_SRC`؛ `INTERVAL_TO_RES`؛ `loadTvScript`؛ النوع `TvChartHandle`؛ الواجهة `TvChartProps`؛ المكوّن `TvChart` (`forwardRef`) | `forwardRef`, `useEffect`, `useImperativeHandle`, `useRef`, `useState` من react؛ أنواع `ChartingLibraryWidgetConstructor`, `ChartingLibraryWidgetOptions`, `IChartingLibraryWidget`, `ResolutionString` من `vendor/tradingview/charting_library/charting_library`؛ `createTradingDatafeed`؛ `cn` | `webui/src/components/trading/GoldChartPanel.tsx`؛ `webui/src/components/trading/TradingChartSidecar.tsx` |
| 136 | `webui/src/components/trading/GoldChartPanel.tsx` (191 سطراً) | صفحة `/chart`: شريط حالة، زر Analyze gold يستدعي `GET /api/trading/analyze?interval=15m&team_mode=core` (مهلة 120 ثانية)، يقتبس `/api/trading/status` و`/api/trading/quote?symbol=XAUUSD` كل 15 ثانية، يطبق الرسوم عبر `applyTradingDrawings`، يعرض `TvChart` + `ChartTradeOverlay` + `AgentCards` | الواجهات `TradingStatus`, `QuotePayload`, `AnalysisResult`؛ `GoldChartPanel`؛ `authHeaders`؛ `refreshMarket`؛ `runAnalysis` | `useCallback`, `useEffect`, `useRef`, `useState`؛ `AgentCards`, `AgentCard`؛ `ChartTradeOverlay`؛ `TradingStatusBar`؛ `TvChart`؛ `applyTradingDrawings`؛ `Button`؛ `useClient`؛ `fetchWithTimeout` | `webui/src/App.tsx` (lazy لمسار `/chart`) |
| 137 | `webui/src/components/trading/TradingRecommendationCard.tsx` (88 سطراً) | بطاقة توصية: القرار، الملخص، الثقة، مستويات Entry/SL/TP1/TP2، أزرار Approve/Reject الورقية عبر `GET /api/trading/paper?recommendation_id=&action=`، قائمة مراحل، ثم `AgentCards` | الواجهة `TradingRecommendationCardProps`؛ `TradingRecommendationCard`؛ `onPaper` | `AgentCards`؛ `TradingStageChecklist`؛ `Button`؛ `TradingResultWire`؛ `useClient`؛ `useCallback`, `useState` | `TradingBriefingPanel.tsx`؛ `TradingInbox.tsx`؛ `TradingPerformance.tsx`؛ `webui/src/components/MessageBubble.tsx` |
| 138 | `webui/src/components/trading/TradingStageChecklist.tsx` (41 سطراً) | قائمة مراحل إنجليزية: ✓ / ✗ / … حسب done/failed/غير ذلك | `TradingStageChecklistProps`؛ `stageLabel`؛ `statusMark`؛ `TradingStageChecklist` | `STAGE_LABEL_EN`؛ `TradingStageWire`؛ `cn` | `TradingRecommendationCard.tsx` |
| 139 | `webui/src/components/trading/AgentCards.tsx` (34 سطراً) | يعرض بطاقات `derive_cards` كـ JSON منسّق داخل مقالات. فارغ: «No recommendation cards yet.» | الواجهة `AgentCard`؛ `AgentCardsProps`؛ `AgentCards` | لا استيراد خارجي | `GoldChartPanel.tsx`؛ `TradingRecommendationCard.tsx` |
| 140 | `webui/src/components/trading/ChartTradeOverlay.tsx` (30 سطراً) | طبقة عائمة أسفل يسار الشارت تعرض action/entry/SL/TP. تختفي إذا لا توصية أو action=wait | الواجهات `TradeRecommendation`, `ChartTradeOverlayProps`؛ `ChartTradeOverlay` | لا استيراد | `GoldChartPanel.tsx`؛ `TradingChartSidecar.tsx` |
| 141 | `webui/src/components/trading/TradingBriefingPanel.tsx` (83 سطراً) | صفحة `/briefing`: يجلب `/api/trading/briefing` كل 30 ثانية ويعرض الملخص والسعر والتوصية المفتوحة داخل `TradingRecommendationCard` + `TradingStatusBar` | الواجهة `BriefingPayload`؛ `TradingBriefingPanel` | `TradingStatusBar`؛ `TradingRecommendationCard`؛ `useClient`؛ `useCallback`, `useEffect`, `useState` | `webui/src/App.tsx` (lazy لمسار `/briefing`) |
| 142 | `webui/src/components/trading/TradingChartBottomSheet.tsx` (34 سطراً) | ورقة سفلية للجوال (`lg:hidden`) تُفتح عندما `getTradingSession(chatId).chartOpen` وتحتوي `TradingChartSidecar` | الواجهة `TradingChartBottomSheetProps`؛ `TradingChartBottomSheet` | `TradingChartSidecar`؛ `Sheet`, `SheetContent`, `SheetTitle`؛ `getTradingSession`, `subscribeTradingSession`؛ `Suspense`, `useEffect`, `useState` | `webui/src/App.tsx` |
| 143 | `webui/src/components/trading/TradingChartSidecar.tsx` (56 سطراً) | شارت جانبي للمحادثة: يشترك في حالة الجلسة، يضبط interval من الجلسة، يطبّق الرسوم عند وصول النتيجة، يعرض `ChartTradeOverlay` | الواجهة `TradingChartSidecarProps`؛ `TradingChartSidecar` | `ChartTradeOverlay`؛ `TvChart`, `TvChartHandle`؛ `applyTradingDrawings`؛ `getTradingSession`, `subscribeTradingSession`؛ `useClient`؛ `useEffect`, `useRef`, `useState` | `webui/src/App.tsx` (جزء workbench + lazy)؛ `TradingChartBottomSheet.tsx` |
| 144 | `webui/src/components/trading/TradingConnect.tsx` (153 سطراً) | شاشة ربط تيليجرام (لصق توكن BotFather ثم `configureChannel` + تمكين) وواتساب (`ChannelQrConnectFlow`). يحدّث الميزات كل 8 ثوانٍ. `channelState` يترجم running/enabled/configured | `TradingConnect`؛ `channelState` | `ChannelQrConnectFlow`؛ `Button`؛ `Input`؛ `configureChannel`, `enableNanobotFeature`, `fetchNanobotFeatures`؛ `NanobotFeatureInfo`, `NanobotFeaturesPayload`؛ `useClient`؛ `MessageCircle`, `Send` من lucide-react؛ `useCallback`, `useEffect`, `useState` | `webui/src/App.tsx` (مسار `/connect` و`/channels`)؛ `webui/src/components/settings/SettingsPage.tsx` |
| 145 | `webui/src/components/trading/TradingInbox.tsx` (110 أسطر) | صفحة `/recommendations` و`/inbox`: قائمة من `GET /api/trading/recommendations` وتفاصيل البطاقة مع موافقة ورقية | الواجهة `InboxRow`؛ `rowToResult`؛ `TradingInbox` | `TradingRecommendationCard`؛ `Button`؛ `TradingResultWire`؛ `useClient`؛ `useCallback`, `useEffect`, `useState` | `webui/src/App.tsx` |
| 146 | `webui/src/components/trading/TradingPerformance.tsx` (93 سطراً) | صفحة `/performance`: يجلب `/api/trading/performance` كل 60 ثانية. بطاقات: الإجمالي، المفتوح، المغلق، إجراءات الورق، وعدّ buy/sell/wait، ثم أحدث التوصيات | الواجهة `PerformancePayload`؛ `TradingPerformance`؛ `StatCard` | `TradingRecommendationCard`؛ `useClient`؛ `useCallback`, `useEffect`, `useState` | `webui/src/App.tsx` |
| 147 | `webui/src/components/trading/TradingStatusBar.tsx` (41 سطراً) | شريط مختصر من `/api/trading/briefing` كل 30 ثانية: الملخص واتجاه التوصية المفتوحة | الواجهة `Briefing`؛ `TradingStatusBar` | `useClient`؛ `useCallback`, `useEffect`, `useState` | `GoldChartPanel.tsx`؛ `TradingBriefingPanel.tsx` |
| 148 | `webui/src/lib/trading/session-store.ts` (85 سطراً) | حالة تداول في الذاكرة لكل `chatId`: chartOpen, interval, stages, result. اشتراك المستمعين. مفتاح جزء الشارت `trading-chart:${anchorPaneKey}` | `DEFAULT_STATE`؛ `sessions`؛ `listeners`؛ `snapshot`؛ `notify`؛ `getTradingSession`؛ `subscribeTradingSession`؛ `openTradingChart`؛ `pushTradingStage`؛ `setTradingResult`؛ `resetTradingSession`؛ `TRADING_CHART_PANE_PREFIX`؛ `tradingChartPaneKey`؛ `isTradingChartPaneKey`؛ `anchorPaneKeyFromTradingChart` | أنواع `TradingResultWire`, `TradingSessionState`, `TradingStageWire` | `webui/src/App.tsx`؛ `webui/src/hooks/useNanobotStream.ts`؛ `TradingChartSidecar.tsx`؛ `TradingChartBottomSheet.tsx` |
| 149 | `webui/src/lib/trading/stage-labels.ts` (14 سطراً) | تسميات إنجليزية للمراحل: market_data, structure, liquidity, supply_demand, multi_timeframe, news, risk, final_decision, drawing, execution_guard, general, research | `STAGE_LABEL_EN` | لا استيراد | `TradingStageChecklist.tsx` |
| 150 | `webui/src/lib/trading/types.ts` (43 سطراً) | أنواع السلك الأمامية المطابقة لـ `result_to_wire` | `TradingStageWire`؛ `TradingRecommendationWire`؛ `TradingResultWire`؛ `TradingSessionState` | لا استيراد | `session-store.ts`؛ `useNanobotStream.ts`؛ `TradingRecommendationCard.tsx`؛ `TradingStageChecklist.tsx`؛ `TradingInbox.tsx` |
| 151 | `webui/package.json` (68 سطراً) | بيان حزمة `nanobot-webui` 0.1.0. scripts: `dev`=vite، `build`=tsc ثم vite build، `preview`، `test`=vitest run، `test:coverage`، `test:watch`، `lint` عبر eslint الجذر. dependencies: `@radix-ui/react-alert-dialog` ^1.1.4، `@radix-ui/react-dialog` ^1.1.4، `@radix-ui/react-dropdown-menu` ^1.1.4، `@radix-ui/react-popover` 1.1.15، `@radix-ui/react-select` 2.2.6، `@radix-ui/react-slot` ^1.1.1، `@radix-ui/react-tooltip` ^1.1.6، `class-variance-authority` ^0.7.1، `clsx` ^2.1.1، `diff` ^9.0.0، `i18next` ^26.0.6، `lucide-react` ^0.469.0، `micromark-factory-space` ^2.0.1، `qrcode` ^1.5.4، `react` ^18.3.1، `react-dom` ^18.3.1، `react-i18next` ^17.0.4، `react-syntax-highlighter` ^15.6.1، `rehype-katex` ^7.0.1، `remark-breaks` ^4.0.0، `remark-gfm` ^4.0.0، `remark-math` ^6.0.0، `streamdown` 2.5.0، `tailwind-merge` ^2.6.0. devDependencies: `@eslint/js` ^10.0.1، `@tailwindcss/typography` ^0.5.19، `@testing-library/jest-dom` ^6.6.3، `@testing-library/react` ^16.1.0، `@testing-library/user-event` ^14.5.2، `@types/node` ^24.0.0، `@types/qrcode` ^1.5.6، `@types/react` ^18.3.18، `@types/react-dom` ^18.3.5، `@types/react-syntax-highlighter` ^15.5.13، `@vitejs/plugin-react` ^4.3.4، `@vitest/coverage-v8` 2.1.9، `autoprefixer` ^10.4.20، `eslint` ^10.4.0، `eslint-plugin-react-hooks` ^7.1.1، `globals` ^17.6.0، `happy-dom` ^16.3.0، `katex` ^0.16.21، `postcss` ^8.5.0، `tailwindcss` ^3.4.17، `tailwindcss-animate` ^1.0.7، `typescript` ^5.7.2، `typescript-eslint` ^8.59.4، `vite` ^5.4.11، `vitest` ^2.1.8 | لا ينطبق (JSON) | لا ينطبق | `hatch_build.py` / `nanobot/webui/build.py` / Dockerfile مرحلة webui-builder / سكربت النشر `bun` على VPS |
| 152 | `webui/public/charting_library/package.json` (8 أسطر) | بيان مكتبة الرسوم المضمّنة: CL v31.2.0، المعرّف الداخلي `36c4a425f144924fd9ef50c4e2b95f86ab93be96` بتاريخ `2026-05-05T15:09:40.780Z`. المدخل `charting_library.cjs.js` / `charting_library.esm.js` / الأنواع `charting_library.d.ts` | حقول JSON: `private`, `description`, `type`, `main`, `module`, `types` | لا ينطبق | يخدمه الخادم الثابت على `/charting_library/` الذي يحمّله `TvChart.tsx` |

---

## 7. اختبارات التداول وملف البيئة النموذجي (153–170)

| # | المسار | الوظيفة | الكلاسات / الدوال الرئيسية | Imports | يُستخدم بواسطة |
|---:|---|---|---|---|---|
| 153 | `tests/trading/test_lonora_cognition.py` | عقد Lonora: تحويل conditional إلى immediate بعد تحقق الشرط، رفض المستويات غير المدعومة، veto غير قابل للتخزين، رفض قلب الطرف عند إعادة التقييم، تثبيت مسار السيناريو على الهدف/الوقف، `plan_turn` للمتابعة عند وجود خطة حيّة، الـ synthesizer يستخدم LLM مزيفاً ولا يعتمد `selected_candidate`، المتابعة الحيّة لا تستدعي الـ synthesizer، veto لا يخزّن | `_snapshot`؛ `_parsed`؛ `test_apply_model_decision_follow_through_not_conditional`؛ `test_live_already_through_sell_entry`؛ `test_ungrounded_proposed_levels_dropped`؛ `test_gate_veto_is_not_tradable`؛ `test_direction_flip_on_reevaluation_refused`؛ `test_scenario_path_pins_to_target_and_stop`؛ `test_plan_turn_followup_when_live`؛ `test_synthesizer_uses_fake_llm_not_selected_candidate`؛ `test_live_plan_followup_does_not_call_synthesizer`؛ `test_gate_veto_stores_nothing` | `json`؛ `pytest`؛ `apply_model_decision`, `apply_revision`, `entry_print_state`؛ `EvidenceSnapshot`؛ `run_final_decision_synthesizer`؛ `build_drawing_plan`؛ `run_unified_chart_agent`؛ `latest_live_recommendation`, `store_recommendation`؛ `assess_plan_tradability`؛ `plan_turn`؛ أنواع السوق والقرار | مشغّل `pytest` |
| 154 | `tests/trading/test_gold_intent_context.py` | سياق النية: تحليل عربي يحقن كتلة `gold_analysis` و`analyze_gold`؛ تيليجرام «اعطيني توصية» يطلب عدم تكرار البطاقة؛ الدردشة العامة لا تحقن سياقاً | `test_gold_intent_context_for_analysis`؛ `test_gold_intent_context_telegram_does_not_repeat_card`؛ `test_gold_intent_context_skips_general_chat` | `asyncio`؛ `RequestContext`؛ `gold_intent_runtime_context` | مشغّل `pytest` |
| 155 | `tests/trading/test_recommendation_cards.py` | تنسيق البطاقة: تقريب السعر، ترجمة الاتجاه/التحيز/كسر الهيكل، R:R من المستويات، تيليجرام HTML عربي بدون كسور قبيحة وبدون كلمة Opening، واتساب بـ `*توصية:` | `_payload`؛ `test_format_price_rounds_ugly_floats`؛ `test_translate_structure_reasons`؛ `test_compute_rr_from_levels`؛ `test_telegram_card_is_arabic_and_rounded`؛ `test_whatsapp_card_is_arabic` | `compute_rr`, `format_price`, `render_telegram_card`, `render_whatsapp_card`, `translate_reason` | مشغّل `pytest` |
| 156 | `tests/trading/test_intent_router.py` | توجيه النية: سعر إنجليزي → price_query؛ توصية عربية على الذهب → recommendation؛ «منيح اعطيني توصية» بدون كلمة ذهب وبثقة ≥ 0.85؛ «hello there» → general_chat | `test_route_price_query`؛ `test_route_recommendation_arabic`؛ `test_route_recommendation_without_gold_keyword`؛ `test_route_general_chat` | `route_intent` | مشغّل `pytest` |
| 157 | `tests/trading/test_stage_delivery_channels.py` | تسليم المراحل/البطاقة: تيليجرام HTML مرة واحدة؛ واتساب نص عادي؛ تيليجرام يحدّث قائمة واحدة لا agent_ui؛ واتساب سطر تقدّم واحد؛ flush للمهام المعلّقة؛ websocket يبقي مراحل agent_ui | `test_publish_result_sends_telegram_html_card`؛ `test_publish_result_sends_whatsapp_plain_card`؛ `test_telegram_stages_publish_one_checklist_not_agent_ui`؛ `test_whatsapp_stages_publish_single_progress_line`؛ `test_publish_result_flushes_pending_stage_tasks`؛ `test_websocket_keeps_agent_ui_stages` | `asyncio`؛ `AsyncMock`, `MagicMock`؛ `OUTBOUND_META_AGENT_UI`؛ `TRADING_PROGRESS_META`؛ `TradingStagePublisher`؛ `emit_stage` | مشغّل `pytest` |
| 158 | `tests/trading/test_fast_path.py` | المخطِّط والمسار السريع: سعر عربي → market_data_only؛ تحليل+توصية عربي → full_analysis؛ «اعطيني توصية» → full_analysis؛ «شغّل غرفة الأخبار» → preset `gold_news_war_room`؛ ذكر ذهب ضعيف الثقة يُتخطى؛ التوصية لا تُترك لـ LLM الغلاف؛ سعر مع OANDA غير مضبوط يعيد نصاً يذكر OANDA | `test_plan_turn_price_query_arabic`؛ `test_fast_path_runs_analysis_for_arabic_request`؛ `test_fast_path_runs_recommendation_without_gold_keyword`؛ `test_resolve_team_preset_war_room`؛ `test_fast_path_skips_low_confidence_gold_mention`؛ `test_fast_path_recommendation_does_not_skip_to_llm`؛ `test_fast_path_oanda_unconfigured` | `asyncio`؛ `try_gold_fast_path`؛ `resolve_team_preset`؛ `plan_turn` | مشغّل `pytest` |
| 159 | `tests/trading/test_reprice_loop.py` | بعد G7 reanchor إلى 2405 يحدّث الدخول ويعيد G6 | `_minimal_inputs`؛ `test_reprice_loop_updates_entry_after_g7_reanchor` | `asyncio`؛ `GateInputs`, `build_gates`؛ `apply_g7_reprice_loop`؛ `AgentRecommendation`, `EntryPlan`, `GateChainResult`, `GateVerdict`, `LiquidityResult`, `MultiTimeframeResult`, `NewsMacroResult`, `StructureResult`, `SupplyDemandResult` | مشغّل `pytest` |
| 160 | `tests/trading/test_teams.py` | تحميل `gold_debate_desk`: ≥ 3 وكلاء وطبقتان (bull+bear ثم risk) | `test_load_gold_debate_desk_preset` | `load_preset`, `topological_layers` | مشغّل `pytest` |
| 161 | `tests/trading/test_orchestrator.py` | `run_unified_chart_agent(store=False)` يعيد قراراً في {buy, sell, wait} وقائمة مراحل | `test_run_unified_chart_agent_returns_result` (asyncio) | `pytest`؛ `run_unified_chart_agent` | مشغّل `pytest` |
| 162 | `tests/trading/test_forex_factory.py` | بدون تفعيل العلم `fetch_upcoming_events()` يعيد `[]` | `test_forex_factory_disabled_by_default` | `fetch_upcoming_events` | مشغّل `pytest` |
| 163 | `tests/trading/test_trading_tools.py` | `get_gold_quote` يخطئ إن OANDA غير مضبوط؛ `analyze_gold` ينشر نتيجة wait ويزيد نشر الحافلة؛ `sync_emit` بدون bus لا يرمي | `test_get_gold_quote_unconfigured`؛ `test_analyze_gold_publishes_result`؛ `test_stage_publisher_sync_emit_schedules_task` | `json`؛ `AsyncMock`, `MagicMock`, `patch`؛ `pytest`؛ `RequestContext`, `request_context`؛ `AnalyzeGoldTool`, `GetGoldQuoteTool`؛ `emit_stage`؛ `TradingStagePublisher` | مشغّل `pytest` |
| 164 | `tests/trading/test_decision_memory.py` | يسجّل قرار شراء في JSONL مؤقت ثم `list_recent_decisions` و`format_decisions_for_dream` يحتويان الملخص | `test_record_and_list_decisions` | `json`؛ `Path`؛ `format_decisions_for_dream`, `list_recent_decisions`, `record_trade_decision`, `trades_memory_path`؛ `AgentMarketContext`, `AgentRecommendation`, `FinalDecisionResult`, `MarketSync` | مشغّل `pytest` |
| 165 | `tests/trading/test_gold.py` | `is_gold` يقبل XAUUSD وxau/usd وXAU_USD؛ `require_gold("EURUSD")` يرمي `GoldOnlyError`؛ `coerce_to_gold` يعيد الرمز القانوني دائماً | `test_is_gold_accepts_aliases`؛ `test_require_gold_rejects_other_symbols`؛ `test_coerce_to_gold_always_returns_canonical` | `DATA_SYMBOL`, `GoldOnlyError`, `coerce_to_gold`, `is_gold`, `require_gold` | مشغّل `pytest` |
| 166 | `tests/trading/test_stage_events.py` | التسمية العربية لـ market_data تحتوي «السوق»؛ `to_wire` يحمل stage وstatus | `test_stage_label_arabic`؛ `test_emit_stage_wire` | `emit_stage`, `stage_label` | مشغّل `pytest` |
| 167 | `tests/trading/test_gates.py` | تماسك شراء صحيح؛ سلسلة بوابات مع مدخلات ناقصة تُنتج أحكاماً و`allowed is False` لأن G4 مطلوب وغير متاح | `test_validate_entry_coherence_buy`؛ `test_gate_chain_runs` | `pytest`؛ `GateInputs`, `build_gates`؛ `run_gate_chain`؛ `validate_entry_coherence`؛ `EntryPlan` | مشغّل `pytest` |
| 168 | `tests/trading/test_trading_api.py` | HTTP: `/api/trading/status` 200 وفيه XAUUSD وoanda_configured؛ `/api/trading/performance` فيه totalRecommendations وpaperActions؛ `/api/trading/klines` يعيد مفتاح candles حتى دون ضبط | `_request`؛ `test_trading_status_without_oanda`؛ `test_trading_performance_payload`؛ `test_trading_klines_unconfigured` | `websockets.http11.Request`؛ `handle_trading_klines`, `handle_trading_performance`, `handle_trading_status` | مشغّل `pytest` |
| 169 | `tests/trading/test_cards.py` | `derive_cards` لقرار wait يحتوي kind=decision | `test_derive_cards_decision_only` | `derive_cards`؛ `AgentFinalResult`, `AgentRecommendation`, `FinalDecisionResult` | مشغّل `pytest` |
| 170 | `.env.example` (4 أسطر) | نموذج أسماء بيانات السوق فقط. الأسماء الموجودة: `OANDA_API_TOKEN` (فارغ)، `OANDA_ACCOUNT_ID` (فارغ)، `OANDA_ENV` وقيمته النموذجية `practice`. التعليق يذكر أن الاعتمادات نفسها تُستخدم على VPS إنتاج foxagent. لا توجد هنا أسماء `FOREX_FACTORY_ENABLED` ولا `TRADING_NEWS_STUB` (تظهر في الشفرة وستُحصر في المرحلة 5). لم يُفتح ملف `.env` الحقيقي | لا ينطبق | لا ينطبق | `nanobot/trading/config.py` يقرأ الأسماء الثلاثة عبر البيئة؛ سكربتات النشر تتوقع وجودها على الخادم |

---

## 8. ملاحظات ربط الجزء ب (حقائق لا اختصار)

1. **سلطة القرار:** المتخصصون 086–090 و093 و111–112 يجمعون أدلة ومرشحين. `risk.selected_candidate` قائمة جودة فقط. الاختيار في 091–092 ثم `apply_model_decision` (ملف الجزء أ). البوابات 104–109 ترفض أو تعيد تسعير الدخول ولا تقلب buy↔sell.
2. **WAIT:** يخرج من 092 عند حاجز تشغيلي (سياق/نموذج/JSON)، ومن 118 عند عدم وجود خطة حيّة، ومن البوابات عبر عدم التخزين في 119–120. التحليل الناجح لا يُجاب بانتظار تحليلي.
3. **خطة حيّة واحدة:** `store_recommendation(..., session_key=)` يرفض الثانية. المتابعة = `grade_live_recommendation` دون synthesizer ثانٍ.
4. **أسراب YAML:** المهام النصية في 122 لا تستدعي وكلاء الهيكل/الأخبار؛ القرار النهائي دائماً `run_unified_chart_agent`.
5. **اختبارات مذكورة في قوائم قديمة وغير موجودة على القرص:** لا توجد الملفات `tests/trading/test_turn_planner.py` ولا `test_followup.py` ولا `test_cognition.py` ولا `test_team_runtime.py` ولا `test_geometry.py` ولا `test_visual_capture.py` ولا `test_drawings.py` ولا `test_recommendations.py` ولا `test_telegram_delivery.py`. التغطية المقابلة موزعة على 153–169 أعلاه.
6. **رموز بلا مستورد إنتاجي ظهرت أثناء المسح:** `explain_stored_recommendation`؛ استيراد `AnalyzeGoldTool` داخل `trading_team.py`؛ وكيل البوت `news_candle`؛ `DEFAULT_MAX_ATR_DISTANCE`؛ `asyncio` في `chain.py`؛ `UTC` في `news_window.py`؛ `defaultdict` و`AgentFinalResult` في `runtime.py`. تُراجع في المرحلة 6.
7. **أسماء بيئة ظهرت في الجزء ب دون قيم ودون أن تكون في `.env.example`:** `FOREX_FACTORY_ENABLED`؛ `TRADING_NEWS_STUB`. قائمة الأسماء الكاملة في المرحلة 5.

---

## 9. جاهزية المرحلة التالية

المرحلة 4 (The Brain) تبدأ من: خط أنابيب `run_unified_chart_agent`، المؤشرات في `detectors.py`، قواعد buy/sell/wait، النص الحرفي لـ `SYNTH_SYSTEM_PROMPT`، البيانات الحية OANDA، المخاطر، وما يُسمّى backtesting (G5 أُزيل).
